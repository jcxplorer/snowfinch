= header "Sites" do
  = link_to "Add a site", new_site_path

#content
  - if @sites.any?
    %ul.list
      = list_of @sites do |site|
        = link_to site.name, site
  - else
    %p You don't have any sites. Click "Add a site" to create one.
